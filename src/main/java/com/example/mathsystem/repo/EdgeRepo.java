package com.example.mathsystem.repo;

import com.example.mathsystem.entity.graph.Edge;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EdgeRepo extends JpaRepository<Edge, Long> {
}
